default: &default
  store_options:
    # 保持ポリシーを満たすため最古キャッシュエントリの期限を制限
    # max_age: <%= 60.days.to_i %>
    max_size: <%= 256.megabytes %>
    namespace: <%= Rails.env %>

development:
  <<: *default

test:
  <<: *default

production:
  database: cache
  <<: *default
