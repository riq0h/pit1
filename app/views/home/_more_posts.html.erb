<%= render 'shared/post_timeline',
           posts: @posts,
           more_posts_available: @more_posts_available,
           load_more_url: @more_posts_available ? root_path(max_id: @older_max_id) : nil %>