<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) + ' | letter' : 'letter' %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- Googleフォント -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <!-- カスタムスタイル -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      font-weight: 300;
    }
  </style>
</head>
<body class="text-gray-900" style="background-color: <%= background_color %>; --page-background-color: <%= background_color %>;">
  <div class="min-h-screen">
    <%- if flash[:notice].present? %>
      <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-4">
        <div class="flex">
          <div class="ml-3">
            <p class="text-sm text-blue-700"><%= flash[:notice] %></p>
          </div>
        </div>
      </div>
    <% end -%>

    <%- if flash[:alert].present? %>
      <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-4">
        <div class="flex">
          <div class="ml-3">
            <p class="text-sm text-red-700"><%= flash[:alert] %></p>
          </div>
        </div>
      </div>
    <% end -%>

    <%= yield %>
  </div>
</body>
</html>
